export const cartStatuses = {
    IDLE: 'idle',
    PENDING: 'pending',
    CLEARED: 'cleared',
};

export const wishlistStatuses = {
    IDLE: 'idle',
    PENDING: 'pending',
    CLEARED: 'cleared',   
};

export const TOKEN = 'TOKEN';